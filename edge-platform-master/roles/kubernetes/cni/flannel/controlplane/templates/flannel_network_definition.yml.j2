# License: Apache-2.0

---

apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: joiningos-flannel
spec:
  config: '{
    "cniVersion": "0.3.1",
    "name": "joiningos-flannel",
    "type": "flannel",
    "mtu": 1440,
    "ipam": {
      "type": "host-local",
      "routes": [{"dst": "{{ flannel_cidr }}"}]
    }
  }'
